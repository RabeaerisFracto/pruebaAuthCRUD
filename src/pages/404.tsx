import { Link } from "react-router-dom";

export default function NotFoundPage() {
    return <div>No pasa naipe...

        <Link to="/">Volver al inicio</Link>//rapidisimo comparado con un hipervinculo tradicional.
    </div>;
}